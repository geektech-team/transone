name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'packages/transone/**'
      - 'packages/transone-cli/**'
      - 'package.json'
      - 'bun.lock'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          # 固定到本地验证过的版本，避免 latest 漂移导致 install 行为不一致
          bun-version: 1.4.0

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build documentation
        # 文档站由 @geektech/tsone 构建（tsone build 静态多页 + SSR），
        # 框架与 CLI 以 npm 依赖形式安装，无需在 CI 先构建本地包。
        # 站点从 GitHub Pages 项目子路径提供服务（如
        # https://<owner>.github.io/transone/），base 取仓库名。
        # transone / cli / ui / project 四个分区由同一站点提供。
        run: bun run docs:build -- --base=/${{ github.event.repository.name }}/

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
